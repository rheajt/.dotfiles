#!/usr/bin/bash

result=${PWD##*/}          # to assign to a variable
result=${result:-/}        # to correct for the case where PWD=/
DIR_STEM="$(echo $result | tr [a-z] [A-Z])"

tmux new -s $DIR_STEM -n code

